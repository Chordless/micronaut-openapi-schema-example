package no.example;

/** Class-level documentation of custom object (never gets shown anywhere). */
public class CustomObject {

    private String someField;

    /** JavaDoc for property on custom object. */
    public String getSomeField() {
        return someField;
    }
}
